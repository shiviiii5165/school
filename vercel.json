{
  "crons": [
    { "path": "/api/cron/lift-suspensions", "schedule": "0 0 * * *" }
  ],
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && node prisma/seed.js && next build"
}
